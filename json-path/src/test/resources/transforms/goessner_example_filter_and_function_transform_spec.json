{
  "pathMappings": [{
    "source": "$.store.book[?(@.price < 10)].price.max()",
    "target": "$.store."
  }, {
    "source": "$.store.book[*].price.sum()",
    "target": "$.store.totalCostOfBooks"
  }
  ]
}